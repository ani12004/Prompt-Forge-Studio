import { getPrompt } from './app/actions/save-prompt';

async function testGetPrompt() {
    console.log("Testing getPrompt...");

    // Note: This requires a valid session/auth context to run in reality
    // Since we are in the agent environment, we will mostly verify the logic via code review 
    // and ensuring the types and exports are correct.
}
